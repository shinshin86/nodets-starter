const { incrementNum, numConvertToStr, greeding, toggle } = require("./sample");

console.log(incrementNum(1));
console.log(numConvertToStr(100));
console.log(greeding("Test"));
console.log(toggle(true));
